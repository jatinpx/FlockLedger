# Copy to `.env`. Use your real URL (no trailing slash).
#
# Physical device on Wi‑Fi: PC LAN IP — Windows: ipconfig → IPv4 Address
# Android emulator: http://10.0.2.2:8000
# iOS Simulator / this machine only:
EXPO_PUBLIC_API_URL=http://localhost:8000
